kind: pipeline
type: docker
name: cypress-tests

steps:
  - name: install dependencies
    image: cypress/browsers:latest
    commands:
      - npm ci

  - name: run cypress tests
    image: cypress/browsers:latest
    commands:
      - npm run cy:run